# 博客使用指南

## 🎯 快速开始

### 1. 安装Ruby环境
- 访问 [RubyInstaller](https://rubyinstaller.org/downloads/)
- 下载并安装 Ruby+Devkit 3.1.X (x64)
- 重启命令行窗口

### 2. 初始化项目
双击运行 `setup.bat` 或在命令行中执行：
```bash
./setup.bat
```

### 3. 本地预览
安装完成后，访问：http://localhost:4000

## ✍️ 写作工作流

### 创建新文章
1. 双击 `new_post.bat`
2. 输入文章标题
3. 在生成的文件中编写内容

### 发布文章
1. 双击 `publish.bat`
2. 输入提交信息（可选）
3. 等待推送完成

## 📁 项目结构

```
Maysec.github.io/
├── _posts/          # 博客文章
├── _tabs/           # 页面标签
├── _config.yml      # 站点配置
├── assets/          # 静态资源
├── setup.bat        # 环境配置脚本
├── new_post.bat     # 新建文章脚本
└── publish.bat      # 发布脚本
```

## 📝 文章格式

每篇文章都需要以下Front Matter：

```yaml
---
title: "文章标题"
date: 2025-09-19 15:30:00 +0800
categories: [分类1, 分类2]
tags: [标签1, 标签2]
toc: true
description: 文章描述
---

文章内容开始...
```

## 🚀 发布流程

1. **写作** → 创建或编辑 `_posts` 中的文章
2. **预览** → 本地运行 `bundle exec jekyll serve` 查看效果
3. **发布** → 运行 `publish.bat` 推送到GitHub
4. **等待** → GitHub Pages会在几分钟内自动部署

## 🛠️ 常用命令

```bash
# 安装依赖
bundle install

# 本地运行
bundle exec jekyll serve

# 后台运行
bundle exec jekyll serve --detach

# 清理构建文件
bundle exec jekyll clean

# Git提交
git add .
git commit -m "提交信息"
git push origin main
```

## 🌐 访问地址

- **本地预览**: http://localhost:4000
- **线上博客**: https://maysec.github.io

## 💡 小贴士

1. **图片存储**: 建议使用图床或将图片放在 `assets/images/` 目录
2. **文章分类**: 使用有意义的分类和标签便于管理
3. **定期备份**: 虽然GitHub已经是备份，但本地也要注意保存
4. **SEO优化**: 合理使用description和tags提高搜索可见性

## 🔧 故障排除

### 常见问题

1. **Ruby版本问题**
   ```bash
   ruby --version  # 检查版本
   gem --version   # 检查gem版本
   ```

2. **依赖安装失败**
   ```bash
   bundle clean --force
   bundle install
   ```

3. **端口被占用**
   ```bash
   bundle exec jekyll serve --port 4001
   ```

---

🎉 现在你可以开始愉快地写博客了！
